# ----------------------------------------------------------------------
# FLASK CONFIGURATION
# ----------------------------------------------------------------------
FLASK_APP=runserver.py              # The entry point for the Flask application
FLASK_ENV=development               # Set to 'production' for deployment
SECRET_KEY=your_very_long_and_random_secret_key_here # Used for sessions and security. GENERATE A STRONG ONE.

# ----------------------------------------------------------------------
# POSTGRESQL DATABASE CREDENTIALS
# ----------------------------------------------------------------------
# These variables define the individual parts of the connection URI
DB_USER=orbit_user
DB_PASSWORD=orbit_pass
DB_NAME=orbit_rfm_db
DB_HOST=localhost
DB_PORT=5432                    # Default PostgreSQL port

# ----------------------------------------------------------------------
# SQLALCHEMY DATABASE URI (REQUIRED BY FLASK-SQLALCHEMY)
# ----------------------------------------------------------------------
# Format: postgresql://USER:PASSWORD@HOST:PORT/NAME
# The correct syntax for PostgreSQL is 'postgresql://...'
DATABASE_URL=postgresql://orbit_user:orbit_pass@localhost:5432/orbit_rfm_db
# ----------------------------------------------------------------------
# CORS CONFIGURATION
# ----------------------------------------------------------------------
# Specifies which frontend origins are allowed to communicate with this backend API
# For local development, use your frontend's host/port (e.g., React/Next.js default)
FRONTEND_URL=http://localhost:3000